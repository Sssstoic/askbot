"use client";

import LoginPage from "./login/page"; // Import LoginPage component

const HomePage = () => {
  return <LoginPage />; // Render the LoginPage component directly
};

export default HomePage;
